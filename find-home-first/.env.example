# PostgreSQL connection string — required for live database reads.
# When absent, the application falls back to in-memory demonstration data.
# Obtain this value from your Railway PostgreSQL service dashboard.
DATABASE_URL=
