/**
 * SiteFooter — structural landmark only.
 * Visual design is REPLIT-UI ownership.
 */
export default function SiteFooter() {
  return (
    <footer role="contentinfo">
      <p>&copy; {new Date().getFullYear()} Find Home First.</p>
    </footer>
  );
}
